package com.deefrent.rnd.common.data.reamDbconfig

/*
import com.ekenya.rnd.common.data.fingerprint.FingerPrintDataForRealm
import io.realm.RealmConfiguration
import io.realm.RealmMigration

object RealmConfig {
    fun create(): RealmConfiguration {
        return RealmConfiguration.Builder()
            .name("myrealm.realm")
            .schemaVersion(1)

            .migration(RealmMigration { realm, oldVersion, newVersion ->
                            // Perform migration operations here if needed
                        })

            .modules(RealmModule())
            .build()
    }
}*/
